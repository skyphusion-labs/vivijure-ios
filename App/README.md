# Vivijure for iOS (app shell)

SwiftUI shell will live here (XcodeGen `project.yml` + `Vivijure.xcodeproj`).

Skeleton only: the product surface is not implemented yet. Kit first, then panel parity
against host `docs/CONTRACT.md`.
